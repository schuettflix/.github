name: Schüttflix Microservice CI

on:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]

jobs:
  build:
    runs-on:
      - 'self-hosted'
      - 'amd64'

    steps:
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo Hello from Schüttflix
